name: Learning

on: [push]

jobs:
    Job1:

        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v1
    Job2:

        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v1

